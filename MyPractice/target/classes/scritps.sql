CREATE TABLE Employee ( 
ID INTEGER, 
NAME VARCHAR(50), 
SALARY INTEGER );
use test;
INSERT INTO Employee VALUES(1,'Mandy',12000);
INSERT INTO Employee VALUES(2,'Chris',15000);
INSERT INTO Employee VALUES(3,'Henry',10000);
INSERT INTO Employee VALUES(4,'Katy',10000);
INSERT INTO Employee values(5,'Adams',11000);

SELECT * FROM EMPLOYEE;
-- Find second highest salary of Employees 
SELECT * from EMPLOYEE WHERE salary < (SELECT MAX(salary) FROM EMPLOYEE) ORDER BY salary DESC limit 1;
SELECT * FROM EMPLOYEE ORDER BY salary DESC LIMIT 1 OFFSET 0;
SELECT * FROM EMPLOYEE ORDER BY salary DESC LIMIT 1 OFFSET 1;
SELECT DISTINCT SALARY FROM EMPLOYEE ORDER BY salary DESC LIMIT 1 OFFSET 4;

SELECT * from EMPLOYEE WHERE salary = (SELECT DISTINCT salary FROM EMPLOYEE ORDER BY salary DESC LIMIT 1 OFFSET 3);
SELECT salary FROM EMPLOYEE ORDER BY salary DESC LIMIT 2, 1;

SELECT SUBSTRING(first_name, 1, 4) FROM nc_app.nc_user_profile;

