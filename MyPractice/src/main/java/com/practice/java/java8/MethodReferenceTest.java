package com.practice.java.java8;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Predicate;

public class MethodReferenceTest {

	public static void main(String[] args) {
		
		/*
		 * List<Boolean> stringList =new ArrayList<Boolean>(); List<Boolean> booleanList
		 * =new ArrayList<Boolean>();
		 * 
		 * Predicate<Boolean> it = StaticTest::isLegal;
		 * booleanList.stream().anyMatch(it);
		 * 
		 * List<User> list1 =new ArrayList<User>();
		 * booleanList.stream().anyMatch(User::isLegal);
		 * 
		 * stringList.stream().filter(String::isEmpty).
		 */
		
	}
	
	
}
